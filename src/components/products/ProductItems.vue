<template>
<div>
  <li class="card" v-for="product in exampleData" :key="product.name">
    <h1 class="product-name">{{product.name}}: {{product.values}}</h1>
    <p class="product-name">{{product.name}}: {{product.values}}</p>
    <p class="product-name">{{product.name}}: {{product.values}}</p>
    <p class="product-name">{{product.name}}: {{product.values}}</p>
  </li>
  </div>
</template>

<script>

</script>
